[build-system]
requires = [
  "setuptools>=64",
  "wheel",
  "cffi>=1.12",
  "cmake>=3.15",
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
# Build wheels for CPython 3.10–3.13.
build = "cp310-* cp311-* cp312-* cp313-*"
skip = "pp* *-win32 *-manylinux_i686"

